import 'package:flutter/material.dart';

class PersonalizeActorsView extends StatelessWidget {
  const PersonalizeActorsView({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}